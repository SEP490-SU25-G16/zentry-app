<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/student_nav_graph"
    app:startDestination="@id/studentHomeFragment">

    <fragment
        android:id="@+id/studentHomeFragment"
        android:name="com.zentry.app.ui.fragment.student.StudentHomeFragment"
        android:label="Student Home"
        tools:layout="@layout/fragment_student_home">
    </fragment>

    <action
        android:id="@+id/action_student_logout"
        app:destination="@id/signInFragment"
        app:popUpTo="@navigation/student_nav_graph"
        app:popUpToInclusive="true" />

</navigation>