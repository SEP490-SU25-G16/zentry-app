<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" /> <!-- Thêm quyền camera -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- BLE Feature Required -->
    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />
    <application
        android:name=".ZentryApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Zentryapp"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Service -->
        <activity
            android:name=".faceid.ui.activity.FaceIdEnhancerActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Deep link to open Verify Face ID directly -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="zentry" android:host="face-verify" />
            </intent-filter>
        </activity>
        <service
            android:name=".service.BLEAttendanceService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!-- FCM messaging service for realtime notifications -->
        <service
            android:name=".notification.push.FcmMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Face ID Success Activity declaration -->
        <activity
            android:name=".faceid.ui.setting.success.FaceIdSuccessActivity"
            android:exported="false"
            android:launchMode="singleTop" />
            
        <!-- Face ID Info Activity declaration -->
        <activity
            android:name=".faceid.ui.setting.FaceIdInfoActivity"
            android:exported="false"
            android:launchMode="singleTop" />
            
        <!-- Face ID Register Activity declaration -->
        <activity
            android:name=".faceid.ui.setting.StudentSettingRegisterFaceIdActivity"
            android:exported="false"
            android:launchMode="singleTop" />
            
        <!-- Face ID Update Activity declaration -->
        <activity
            android:name=".faceid.ui.setting.StudentSettingUpdateFaceIdActivity"
            android:exported="false"
            android:launchMode="singleTop" />
            
        <!-- Face ID Verify Activity declaration -->
        <activity
            android:name=".faceid.ui.setting.StudentSettingVerifyFaceIdActivity"
            android:exported="false"
            android:launchMode="singleTop" />
    </application>

</manifest>